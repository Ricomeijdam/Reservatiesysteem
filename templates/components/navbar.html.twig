<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a href="{{ path('default') }}"><img class="logo" src="{{ asset('images/logo.png') }}" alt="logo" width="50" height="50"></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
        <li class="nav-item active">
            <a class="nav-link" href="{{ path('default') }}">Home<span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="{{ path('reservatie_index') }}">Reservatie</a>
        </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('bestelling_index') }}">Bestelling</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('factuur_index') }}">Factuur</a>
            </li>
        <li class="nav-item">
            <a class="nav-link" href="{{ path('tafel_index') }}">Tafel</a>
        </li>

            {% if app.user %}
                <li class="nav-item">
                    <a class="p-2 text-dark" href="/user">Profiel</a>
                </li>
            {% else %}

            {% endif %}

            <div class="btn-group btn-group-toggle">
                {% if app.user %}
                    <li class="nav-item">
                        <a class="btn btn-outline-dark my-2 my-sm-0" href="/logout">Logout {{ app.user }}</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="btn btn-outline-dark my-2 my-sm-0" href="/login/">Login</a>
                        <a class="btn btn-outline-dark my-2 my-sm-0" href="/register/">Registreer</a>
                    </li>
                {% endif %}
            </div>
        </ul>
    </div>
</nav>